@using Microsoft.AspNetCore.Identity
@inject UserManager<ApplicationUser> UserManager; 

@if (!User.Identity.IsAuthenticated)

{

    <span style="padding-right:775px;"></span>
    
        <a class="btn btn-outline-success my-2 my-sm-0" asp-controller="Account" asp-action="Login" style="padding-right: 25px;">
        <i class="bi bi-box-arrow-in-right"></i> Log in
    </a>
    <span style="padding: 5px;"></span>

        <a class="btn btn-outline-primary my-2 my-sm-0" asp-controller="Account" asp-action="Register" style="padding-right: 25px;">
        <i class="bi bi-person-plus"></i> Register
    </a>
   
}
else
{
    <span style="padding-right:600px;"></span>
    
    <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="bi bi-person-badge"></i>  Hello @UserManager.GetUserName(User)
                            </a>

                            <div class="dropdown-menu" aria-labelledby="profile-btn">
                                
                                <a class="dropdown-item" asp-controller="Orders" asp-action="Index">  Orders  </a>
                                 <a class="dropdown-item" asp-controller="Account" asp-action="Users">  Users  </a>
                             
                            </div>
                        </li>

  @**  <div class="dropdown">
        <button class="btn btn-outline-success dropdown-toggle" type="button" id="profile-btn" data-toggle="dropdown"
                aria-expanded="false">
            <i class="bi bi-person-badge"></i> Hello @UserManager.GetUserName(User)
        </button>
        <ul class="dropdown-menu" aria-labelledby="profile-btn">
            <li>
                <a class="dropdown-item" asp-controller="Orders" asp-action="Index">
                    <i class="bi bi-list"></i> Orders
                </a>
            </li>
         </ul>
    </div>*@

    <span style="padding: 5px;"></span>
    <form asp-controller="Account" asp-action="Logout">
        <button class="btn btn-outline-danger my-2 my-sm-0">
            <i class="bi bi-box-arrow-in-right"></i> Log out
        </button>
    </form>
}

